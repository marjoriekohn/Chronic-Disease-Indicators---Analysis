source("renv/activate.R")

# Always start in the project root
if (requireNamespace("here", quietly = TRUE)) {
  setwd(here::here())
  message("📦 renv activated; run source('R/setup.R') to load libraries.")
}
